import React from 'react'
import styled from 'styled-components'

const Wrapper = styled.div``

type Props = Readonly<{
  example: string
}>

export const Strap: React.FC<Props> = ({ example }: Props) => <Wrapper></Wrapper>
