const createTheme = () => {
    const theme = {
      code: `
      # Headline

      ## Subheadline

      \`code?\`
      `
    }
    return theme
  }
  
  const theme = createTheme()
  export default theme